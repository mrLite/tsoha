<%= form_for([@project, @member]) do |f| %>
  <% if @member.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@member.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @member.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
  	<%= f.label :member_role_id %>
	<%= f.select(:member_role_id, MemberRole.find_or_create_roles) %>
  </div>
	<%= f.hidden_field :user_id, { :value => params[:user_id] } %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
